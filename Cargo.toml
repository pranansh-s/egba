[workspace]
members = [
    "egba-core",
    "egba-ui",
    "egba-debugger",
    "emulator",
]

resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = false
overflow-checks = false